id: no-terminology-tsx
language: tsx
files:
  - "**/*.tsx"

ignores:
  - node_modules
  - .docusaurus
  - build
rule:
  any:
    - kind: string_fragment
      matches: terminology
    # Also match template strings (template literals)
    - kind: template_string
      matches: terminology

message: no-terminology
severity: error